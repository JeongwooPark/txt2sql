# Phase J — RAG shrink

RAG remains last-resort after Fast Path / semantic-v2 / legacy router / SQP.
READY plans use `fallback_reason=emergency_after_ready_miss` and require IR/logical/binding snapshots.
Results tagged `execution_source=rag_sql`.
