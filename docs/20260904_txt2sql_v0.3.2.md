# txt2sql 0.3.2 (2026-09-04)

제품 버전 **0.3.2**. Semantic Architecture v2 후속 작업(PlaceScope·Query Contract·plan repair·골드테스트1/2)을 `master`에 통합한다.

## 1. 요약

| 영역 | 내용 |
|------|------|
| PlaceScope | 행정동 BND, D010/D198 grain 정책 중앙화 |
| Query Contract | `verify_query_contract` — SQP·semantic_v2 공통 게이트 |
| Plan repair | Contract span 기반 deterministic repair (Q-ID 하드코딩 없음) |
| Fallback | `RANGE_BOUND`/`PREDICATE` soft-warning 후 실행, hard error만 fallback |
| 평가 | 골드테스트1(구질문)·골드테스트2(newset500) 분리 실행·비교 |
| 인코딩 | `run.py` UTF-8 `_safe_print` / `--log` |

## 2. 아키텍처

```text
질문 → extract_contract
     → (meta는 executable contract면 차단)
     → legacy route / semantic_v2 / SQP
     → validate → (repairable이면 plan_repair 1회)
     → compile_with_contract_gate
     → soft RANGE/PRED → execute | hard → fallback/clarify
```

핵심 모듈:

- `txt2sql/query_contract.py` — 통합 계약 검증, `contract_is_executable_query`
- `txt2sql/semantic_plan/plan_repair.py` — contract→filter/agg/group repair
- `txt2sql/dataset_grain.py` — D010 vs D198 grain
- `txt2sql/semantic_catalog/place_scope.py` — PlaceScope 바인딩

## 3. 골드테스트 (rediag, 2026-09-02)

| 세트 | 결과 | baseline(contract_fix_v2) 대비 |
|------|------|----------------------------------|
| 골드테스트1 | **268/500 (53.6%)** | 248 → +20 |
| 골드테스트2 | **331/500 (66.2%)** | 322 → +9 |

공통 구조 병목: `PREDICATE_DROPPED`, `ENTITY_SELECTION_ERROR`.  
상세: `artifacts/evaluation/gt1_gt2_compare.json`, 캔버스 `gold-test-rediag-structural`.

## 4. 실행

```powershell
# 골드테스트1
.\.venv\Scripts\python.exe -m tests.map_ui_gold500.run `
  --driver api --start-server --url http://127.0.0.1:8009/map `
  --questions tests/map_ui_gold500/questions_gold_test1.json `
  --out artifacts/evaluation/gold_test1_rediag.json `
  --log artifacts/evaluation/gold_test1_rediag.log

# 골드테스트2
.\.venv\Scripts\python.exe -m tests.map_ui_gold500.run `
  --driver api --start-server --url http://127.0.0.1:8009/map `
  --questions tests/map_ui_gold500/questions_gold_test2.json `
  --out artifacts/evaluation/gold_test2_rediag.json `
  --log artifacts/evaluation/gold_test2_rediag.log

# 비교
.\.venv\Scripts\python.exe scripts\compare_gold_tests.py `
  artifacts/evaluation/gold_test1_rediag.json `
  artifacts/evaluation/gold_test2_rediag.json
```

## 5. 버전·브랜치

| 항목 | 값 |
|------|-----|
| 제품 | **0.3.2** |
| 패키지 | `txt2sql` (`pyproject.toml`) |
| FastAPI | `txt2sql/webapp/app.py` `version="0.3.2"` |
| 통합 브랜치 | `semantic-architecture-v2` → `master` |
| 이전 | 0.3.0 (master), 0.3.1 (map-ui newset500 안정화 브랜치) |

## 6. 관련 문서

- `README.md` — 제품 개요·0.3.2 요약
- `docs/작동방식_및_알고리즘.md` — 파이프라인 시나리오
- `docs/Semantic_Query_Plan_구현.md` — SQP + contract/repair
- `docs/고도화_llm2_geodb_to_llm2sql.md` — 버전 이력
- `docs/map_ui_newset500_round_issues_v0.3.1.md` — 0.3.1 회차 기록 (역사)
